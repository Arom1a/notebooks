## 1 HW1

### 1.1 `/` is of type `real * real -> real`

`div` is of type `int * int -> int`

`1 div 0` is of type `int` but no value

## 2 HW2

### 2.1 evaluation should be shown step by step

```sml
val (x, y) : int * int = (33, 3)
    (* [33/x], [3/y] *)
val z : int = 51  
    (* [33/x], [3/y], [51/z] *)
val x * y + z = 150 
```

## 3 HW3

### 3.1 functions' types don't have `fn`

### 3.2 not paying attention to the enum name
